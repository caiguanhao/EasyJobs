= render 'menu'

.panel.max
  %h3= "Deleting #{@job.name}"
  .confirm
    Are you sure you want to delete this job?
    .buttons
      = link_to 'Yes. Delete this job. I know this action is irreversible.', job_path(@job), method: :delete, class: "button button-red"
