= render 'menu'

.panel.max
  %h3= t("Deleting_Admin", admin: @admin.username)
  .confirm
    = t('Delete_Admin_Confirm')
    .buttons
      = link_to t('Delete_Admin_Yes'), admin_path(@admin), method: :delete, data: { confirm: t('Delete_Admin_Confirm') }, class: "button button-red"
